<%= fight.fighter_one_name %>
   <%=  image_tag(fight.fighter_one_image.url, width: "50px") %>
    <%= f.input :fighter %>
	<%= fight.fighter_two_name %>
   <%=  image_tag(fight.fighter_two_image.url, width: "50px") %>
    <%= f.input :fighter %>
    <%= f.input :how %>
    <%= f.input :when %>